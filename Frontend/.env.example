# API base URL (예시)
# VITE_API_BASE_URL=http://localhost:8080

# 손 촬영 화면이 붙는 스캔 서버(scan/server.py) 주소.
# Backend의 application.yml에 있는 analysis.server.url과 반드시 같은 포트로 맞춰야 한다.
# VITE_SCAN_SERVER_URL=http://localhost:8003
